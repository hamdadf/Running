<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="SplashScreenTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item  name="android:windowSplashscreenContent">@drawable/logo2

        </item>
        <item name="android:scaleHeight">40dp</item>
        <item name="android:scaleWidth">40dp</item>


    </style>

    <style name="buttonstyle1" parent="Theme.AppCompat.Light">
        <item name="colorControlHighlight">@color/colorBTN</item>
        <item name="colorButtonNormal">@color/colorBTN</item>
    </style>

</resources>
